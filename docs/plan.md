# LuvTALK 开发一体化计划（v3.0）

> 更新日期：2026-03-10  
> 适用阶段：开发阶段（非线上）  
> 核心目标：在保证安全性和可用性的前提下，保持开发环境与部署后功能和体验一致，删除冗余护栏逻辑。

## 0. 开发阶段总原则（简化版）

| 原则     | 约束                                                | 落地方式                                                   |
| -------- | --------------------------------------------------- | ---------------------------------------------------------- |
| 环境一致 | 开发与部署使用同一套模型路由、Prompt 契约、接口行为 | 禁止仅在开发环境生效的业务分支逻辑                         |
| 安全可用 | 保留必要安全与容错，不引入复杂治理框架              | 仅保留输入校验、超时与回退、状态一致性、回归清单           |
| 删繁就简 | 不做多层开关矩阵、不做复杂灰度设计                  | 当前阶段不新增额外“回滚骨架”与复杂门禁流程                 |
| 小步迭代 | 每次改动可验证、可回归、可记录                      | 一次只推进 1-2 个核心任务，完成即更新 `plan.md` + `log.md` |

## 1. 最小安全与可用基线（必须满足）

| 链路       | 最小要求                         | 验收标准                                                |
| ---------- | -------------------------------- | ------------------------------------------------------- |
| 文本对话   | 请求参数合法、响应结构稳定       | 不出现空回复、字段缺失导致前端异常                      |
| 语音对话   | 录音上传、转写、回复、TTS 可闭环 | 不出现双气泡、残留 thinking、错乱中间态                 |
| 模型回退   | 主模型失败后按固定顺序回退       | `gpt-5.2` 失败后切 `deepseek-reasoner -> deepseek-chat` |
| 会话连续性 | 会话切换、历史恢复状态一致       | 不丢消息、不串会话、不会话闪烁                          |
| 学习闭环   | 收藏、复习、建议输出可用         | 建议可执行，格式稳定，不简化成空泛回复                  |
| 错误处理   | 超时和失败可解释、可恢复         | 用户可继续对话，不被阻塞在不可恢复状态                  |

## 2. 模块规划与约束

| 状态        | 模块 ID | 模块名                           | 核心目标                         | 影响范围                                        | 简化约束                                            |
| ----------- | ------- | -------------------------------- | -------------------------------- | ----------------------------------------------- | --------------------------------------------------- |
| In Progress | MOD-01  | Tutor Voice Engine v2            | 提升语音真实感与等待体验一致性   | `server: voice-tutor/conversation`, `web: chat` | 不改核心协议，优先修复体验一致性问题                |
| In Progress | MOD-02  | Prompt Orchestrator              | 多模式 Prompt 质量稳定与版本可控 | `server: prompt pipeline`                       | 默认 `stable`，实验仅通过 `PROMPT_PROFILE` 显式开启 |
| Planned     | MOD-03  | Teaching Strategy Layer          | 建议更可执行、避免泛化回复       | `server: conversation/review`                   | 单轮建议控制 1-2 条，不增加教学负担                 |
| Planned     | MOD-04  | Premium Minimal UX Kit           | 高级简约视觉与交互统一           | `web: chat/layout/token`                        | 不做花哨动画，避免闪烁和眩晕                        |
| Planned     | MOD-05  | Engagement Loop Lite             | 轻量激励提升学习连续性           | `web+server: achievement/review`                | 不引入复杂社交玩法                                  |
| In Progress | MOD-06  | Session Memory & Learner Profile | 会话记忆与个性化学习画像         | `server: conversation/prompt/voice`             | 主链路不做重计算，画像异步提炼                      |

## 3. 当前任务看板（Now / Next）

| 状态 | 优先级 | 编号   | 任务                    | 交付物                       | 验收标准                  |
| ---- | ------ | ------ | ----------------------- | ---------------------------- | ------------------------- |
| [x]  | P0     | DS-01  | 回退顺序固定            | 服务端路由逻辑               | 回退顺序稳定且可复现      |
| [x]  | P0     | DS-02  | DS 回退教学质量增强     | fallback prompt + 结构化补偿 | 回退回复包含场景与建议    |
| [x]  | P0     | PRM-01 | Prompt 分层模板化       | `base/mode/level/safety`     | 输出结构稳定              |
| [x]  | P0     | PRM-02 | Prompt Profile 开关     | `PROMPT_PROFILE` + 单测      | 默认行为零差异            |
| [x]  | P0     | PRM-03 | 输出 schema 补偿        | normalizer + service guard   | 字段缺失自动补齐          |
| [x]  | P0     | VOI-16 | 语音状态一致性修复      | 前后端状态机修复             | 无双气泡、无残留 thinking |
| [ ]  | P0     | VOI-11 | TTS 韵律模板            | 韵律规则 + 样本              | 听感自然度提升            |
| [ ]  | P0     | VOI-13 | 发音点评微模板          | 点评模板 + 映射规则          | 建议短且可执行            |
| [ ]  | P0     | PRM-04 | Prompt 评测集与评分脚本 | 50+ 场景 + 自动评分          | Prompt 变更可自动回归     |
| [ ]  | P0     | MEM-02 | L1/L2 memory pack 注入  | 后端注入实现 + 埋点          | 记忆生效且无明显时延回退  |
| [ ]  | P1     | UX-12  | 聊天与语音条视觉统一    | token 清单 + 页面改造        | 高级简约且无闪烁眩晕      |
| [ ]  | P1     | VOI-14 | 语音评测脚本            | 延迟/失败率/MOS 输出         | 能稳定出周报指标          |

## 4. 近两周执行计划（可直接开工）

| 周期   | 目标                       | 核心任务                                              | 退出条件                            |
| ------ | -------------------------- | ----------------------------------------------------- | ----------------------------------- |
| Week 1 | 稳定语音教学体验           | `VOI-11`、`VOI-13`、`UX-12`（先做等待条与语音条统一） | 语音链路无中间态错乱，体验连贯      |
| Week 2 | 提升 Prompt 可持续迭代能力 | `PRM-04`、`MEM-02`                                    | Prompt 变更可自动回归，记忆注入可控 |

## 5. 文档协作规则（单一计划源）

| 文档             | 规则                                                 |
| ---------------- | ---------------------------------------------------- |
| `docs/plan.md`   | 唯一计划与模块约束文档，路线图与任务都在此维护       |
| `docs/log.md`    | 记录关键修复点、价值点、设计决策，不记录冗余过程噪音 |
| `docs/deploy.md` | 仅维护部署所需配置与步骤，新增变量必须同步           |

## 6. 下一步（本次会后）

| 优先级 | 行动项                            | 结果定义                       |
| ------ | --------------------------------- | ------------------------------ |
| P0     | 启动 `VOI-11`（TTS 韵律模板）     | 导师语音自然度提升且风格稳定   |
| P0     | 启动 `PRM-04`（评测集与自动评分） | Prompt 质量有自动化回归基线    |
| P0     | 启动 `MEM-02`（L1/L2 注入）       | 上下文记忆可用且不拉高等待焦虑 |
