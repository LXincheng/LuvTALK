{
  "name": "luvtalk-monorepo",
  "private": true,
  "packageManager": "pnpm@10.3.0",
  "scripts": {
    "dev": "turbo run dev --parallel --filter=web --filter=server",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
