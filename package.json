{
  "name": "luvtalk-monorepo",
  "private": true,
  "packageManager": "pnpm@10.3.0",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
